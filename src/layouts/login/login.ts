const tpl = `
<main class="sp-wrapper">
      <form class="sp-wrapper--small">
       {{{header}}}

        <div class="sp-wrapper--group">
          {{{form}}}
        </div>
        
        {{{action}}}
      </form>
    </main>
`

export default tpl
